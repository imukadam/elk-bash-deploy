input {
    file {
        path => "/var/log/kibana/kibana.stdout"
        codec => "json"
    }
    }

output {
    elasticsearch {
        hosts => [ "localhost:9200" ]
        index => "kibana_logs"
    }
}
